{
  "$schema": "https://transtyle.dev/schemas/config/v0.json",
  "name": "component-tier-fixture",
  "tokens": [
    "tokens/base.tokens.json",
    "tokens/override.tokens.json"
  ],
  "modes": {
    "color-scheme": { "values": ["light"], "default": "light" }
  },
  "derivation": {
    "rules": "standard@1",
    "autoDark": false,
    "require": ["semantic.color.primary"]
  },
  "targets": {}
}
