// Preview wiring from the transtyle-built output: the Scheme toolbar, the mode
// decorator, DS-canvas backgrounds, and the sibling stylesheets (the shadcn
// variables give the text story its colors; the daisyUI file is Tailwind-build
// input and stays inert here — this demo is about Storybook's own chrome).
export * from '../../../dist/storybook/preview.transtyle';
