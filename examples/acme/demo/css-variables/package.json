{
  "name": "acme-demo-css-variables",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "transtyle build --cwd ../..",
    "dev": "vite --port 4105",
    "prebuild": "transtyle build --cwd ../..",
    "build": "vite build",
    "preview": "vite preview --port 4105"
  },
  "devDependencies": {
    "@transtyle/cli": "*",
    "vite": "^6.0.0"
  }
}
