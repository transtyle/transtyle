// Preview composition from the transtyle-built output: sibling stylesheets
// (shadcn + daisyUI), the Scheme toolbar, the mode decorator, DS backgrounds.
import './tailwind.css';
export * from '../../../dist/storybook/preview.transtyle';
