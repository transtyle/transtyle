{
  "name": "govuk-demo-bootstrap",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "transtyle build --cwd ../..",
    "dev": "vite --port 4301",
    "prebuild": "transtyle build --cwd ../..",
    "build": "vite build",
    "preview": "vite preview --port 4301"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "@transtyle/cli": "0.1.0-alpha.0",
    "sass": "^1.77.0",
    "vite": "^6.0.0"
  }
}
